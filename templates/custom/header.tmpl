<style>
    :root {
        /* Spacing */
        --spacer: 1rem;
        --spacer-1: .25rem;
        --spacer-2: .5rem;
        --spacer-3: var(--spacer);
        --spacer-4: 1.5rem;
        --spacer-5: 3rem;

        --border-radius: .33rem;
        --color-border: var(--color-secondary);
        --transition-speed: 150ms;
    }

  #navbar .brand #navbar-expand-toggle {
    margin-left: var(--spacer-4);
  }

  /* Hide brand navbar completely when not in mobile. */
  @media (min-width: 768px) {
    #navbar .brand {
      display: none;
    }
  }
</style>
